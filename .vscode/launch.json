{
    //VS Code environment variables file to use -only- when local debugging with VS Code
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "export": "GIN_MODE=release",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/src",
            "env": {
                "PORT": "10000",
                "SQL_CONNECTION_STRING_EV":"",
            },
            "args": []
        }
    ]
}